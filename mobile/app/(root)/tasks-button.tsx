// app/(root)/tasks-button.tsx
import React from "react";

// Placeholder screen so the tab exists. We never navigate here;
// the tab's custom button just opens your modal.
export default function TasksButtonScreen() {
  return null;
}